import React from 'react';
import UserCard from '../components/UserCard';

const MainContainer = () => {
  return (
    <div>
      Main Container
      <UserCard/>
    </div>
  );
}

export default MainContainer;
